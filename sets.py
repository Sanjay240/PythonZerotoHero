s = set() # define set it only takes unique values

l1 = [1,2,3,4]
l2 = [2,3,4,5]
set_from_list = set(l1) # only takes one argument at time
print(set_from_list)

# set_from_list.add(l2) through the error unhashaable type :'list'
# print(set_from_list) 